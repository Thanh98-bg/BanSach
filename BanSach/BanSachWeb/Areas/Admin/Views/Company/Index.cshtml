@model IEnumerable<Company>
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2>Index Company</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Company" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Create New Company
            </a>
        </div>
    </div>
    <br />
    <br />
    <table class="table table-bordered table-striped" id="tblData">
        <thead>
        <tr>
            <th>Name</th>
            <th>StreetAddress</th>
            <th>City</th>
            <th>State</th>
            <th>PostalCode</th>
            <th>PhoneNumber</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
@*            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.StreetAddress</td>
                    <td>@item.City</td>
                    <td>@item.State</td>
                    <td>@item.PostalCode</td>
                    <td>@item.PhoneNumber</td>
                    <td>
                        <a asp-controller="Company" asp-action="Edit" class="btn btn-primary" asp-route-id="@item.Id">
                            <i class="bi bi-pencil"></i>
                        </a>
                        <a asp-controller="Company" asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Id">
                            <i class="bi bi-trash3"></i>
                        </a>
                    </td>
                </tr>
            }*@
        </tbody>
    </table>
</div>
@section Scripts{
    <script src="~/js/company.js"></script>
}